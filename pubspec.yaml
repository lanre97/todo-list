name: todo_list
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^7.3.3
  firebase_core: ^1.10.0
  cloud_firestore: ^3.1.0
  intl: ^0.17.0
  flutter_colorpicker: ^0.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  fonts:
    - family: Kristen
      fonts:
        - asset: assets/fonts/kristen.ttf
